name: Build Canvas

on:
  push:
    branches:
      - main
    paths:
      - 'canvas/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build-app.yml
    with:
      app-name: canvas
      app-folder: canvas
    secrets: inherit